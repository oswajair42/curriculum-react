import React from "react";
import Curriculum from "./Curriculum"; // Importamos el componente

function App() {
  return (
    <div>
      <Curriculum /> {/* Renderizamos el currículum encapsulado */}
    </div>
  );
}

export default App;
